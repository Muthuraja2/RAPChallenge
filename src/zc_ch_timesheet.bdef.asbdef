projection implementation in class ZCL_CH_TIMESHEET_PROJ unique;
strict ( 2 );
use draft;
define behavior for ZC_CH_TIMESHEET alias Timesheet
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}